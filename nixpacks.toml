[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.install]
cmds = [
  "npm install",
  "pip install --break-system-packages python-telegram-bot==20.7 pyotp aiohttp"
]

[start]
cmd = "bash start.sh"
